export class Room {
    description: string;
    imageURL: string;

    constructor(description: string, imageURL: string) {
        this.description = description;
        this.imageURL = imageURL;
    }
}